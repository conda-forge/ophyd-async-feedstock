{% set name = "ophyd-async" %}
{% set version = "0.19.1" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/ophyd_async-{{ version }}.tar.gz
  sha256: a98f3514e159b3777b9eaf3b9417a2238c4fefc5c657a82cb043174c569acc37

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=64
    - setuptools-scm >=8
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - bluesky-base >=1.13.1rc2
    - event-model >=1.23
    - pyyaml
    - colorlog
    - pydantic >=2.0
    - pydantic-numpy
    - stamina >=23.1.0
    - scanspec >=0.8
    - velocity-profile
    - h5py
    - aioca >=2.0
    - p4p >=4.2.2
    - pytango >=10.0.2

test:
  imports:
    - ophyd_async
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://blueskyproject.io/ophyd-async
  summary: Asynchronous Bluesky hardware abstraction code, compatible with control systems like EPICS and Tango
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thopkins32
    - mrakitin
    - jwlodek
